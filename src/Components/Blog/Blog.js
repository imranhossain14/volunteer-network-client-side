import React from 'react';

const Blog = () => {
    const styles = {
        textAlign: 'center',
        color: 'red'
    }
    return (
        <div>
           <h1 style={styles}>The page is under construction!!!!!!</h1>
        </div>
    );
};

export default Blog;